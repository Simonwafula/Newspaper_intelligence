# Optional ML Dependencies for Advanced Layout Detection
# Install with: pip install -r requirements-ml.txt
#
# These are not required for the base system (advanced_layout_enabled=False)
# Only install when you want to enable ML-based features

# ========== PHASE 3: ML LAYOUT DETECTION ==========
# PyTorch (required by LayoutParser and Detectron2)
torch>=2.0.0,<3.0.0
torchvision>=0.15.0

# LayoutParser with Detectron2 backend for layout detection
layoutparser[layoutmodels,effdet]>=0.3.4
# Note: layoutparser[layoutmodels] includes Detectron2

# Required by LayoutParser
opencv-python>=4.8.0
pillow>=9.0.0

# ========== PHASE 4: ADVANCED OCR ==========
# Block-level OCR with word coordinates
paddleocr>=2.7.0
paddlepaddle>=2.5.0  # CPU version of PaddlePaddle

# ========== PHASE 6: SEMANTIC GROUPING (Future) ==========
# Uncomment when ready to implement BGE embeddings:
# sentence-transformers>=2.3.0

# ========== OPTIONAL: PERFORMANCE OPTIMIZATION ==========
# Uncomment for faster inference:
# onnxruntime>=1.16.0  # For ONNX model inference (faster CPU)
# openvino>=2023.0     # Intel optimization
